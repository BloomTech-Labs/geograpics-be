paths:
 /users:
   get:
     summary: Endpoint to get User Information
     description: This Endpoint will retrieve user information (profile pic, email etc) for logged in User.
     tags:
       - Users
     consumes:
       - "application/json"
     produces:
       - "application/json"
    #  parameters:
    #    - in: body
    #      name: body
    #      description: "Data Needed to Register"
    #      required: true
    #      schema:
    #        type: ojbect
    #        properties:
    #          name:
    #            type: string
    #          email:
    #            type: string
    #          username:
    #            type: string
    #          password:
    #            type: string
    #            format: password
     responses:
       200:
         description: Authentication Successful
        #  schema:
        #    type: object
        #    $ref: '#/definitions/Professor'
       500:
          description: Could Not Authenticate
